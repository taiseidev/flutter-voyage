import 'package:flutter/material.dart';

class UseReducerSample extends StatelessWidget {
  const UseReducerSample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
